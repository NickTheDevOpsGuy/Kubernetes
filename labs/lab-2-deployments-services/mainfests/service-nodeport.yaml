apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80        # Service port
      targetPort: 80  # Pod containerPort
      nodePort: 30007 # NodePort (optional, can let K8s pick)
      protocol: TCP